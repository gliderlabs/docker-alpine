export RELEASE="v3.6"
export ARCH="ppc64le"
export MIRROR="http://dl-cdn.alpinelinux.org/alpine"
export PACKAGES="alpine-baselayout,busybox,alpine-keys,apk-tools,libc-utils"
export BUILD_OPTIONS=(-b -s -t UTC -r $RELEASE -m $MIRROR -p $PACKAGES -a $ARCH)
export TAGS=(alpine:3.6-${ARCH})
